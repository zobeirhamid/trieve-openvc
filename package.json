{
  "name": "trieve-openvc",
  "private": true,
  "license": "MIT",
  "version": "1.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "populate": "bun scripts/populate/populate.ts",
    "test": "bun test --timeout 100000"
  },
  "devDependencies": {
    "cheerio": "^1.0.0-rc.12"
  }
}